package com.epam.training.oop.entity.criteria.deviceCriteria;

import com.epam.training.oop.entity.criteria.SearchCriteria;

public enum RefrigeratorCriteria implements SearchCriteria {
    POWER_CONSUMPTION,
    WEIGHT,
    FREEZER_CAPACITY,
    OVERALL_CAPACITY,
    HEIGHT,
    WIDTH
}
